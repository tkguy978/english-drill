# 영어 문장 연습 앱

## 목적
한글 문장을 보고 스스로 영어로 말한 뒤, 클릭해서 정답을 확인하는 연습 도구.
블로그 글에서 링크로 유입되는 사용자가 바로 연습할 수 있어야 한다.

## 핵심 규칙
- SRS(간격 반복) 아님. 섹션을 선택하면 그 섹션의 모든 문장을 순회한다.
- 로그인 없음. 진도와 표시는 localStorage에만 저장한다.
- 빌드 도구 없이 동작하는 정적 사이트로 유지한다. (index.html + 모듈 JS)
- 외부 라이브러리 추가 금지. 필요하면 먼저 물어볼 것.

## 데이터 형식
data/*.tsv — 헤더 없음, 탭 구분, 열 순서 고정:
번호 / 자료 / 섹션 / 한글 / 영어

data/manifest.json — 자료 > 그룹(에피소드) > 섹션(장면) 세 단계.
각 단계에 표시용 제목을 두고, 섹션에는 TSV 파일 이름을 적는다.
"자료"는 미드, 영화, 교재 등 문장의 출처를 가리킨다. 코드에서는 source 다.

## URL 규약
?source=friends&section=s01e01_01 로 특정 세트를 바로 연다.
&mode=marked 를 붙이면 헷갈림으로 표시한 문장만 연다.
표시한 문장이 없으면 전체를 열고 그 사실을 알린다.
파라미터가 없으면 섹션 선택 화면을 보여준다.

## 작업 방식
- legacy/ 는 이전 버전 참고용이다. 수정하거나 import 하지 말 것.
- 데이터 파일(*.tsv)의 문장 내용은 임의로 고치지 말 것.